name: Hello

on: 
  push:
    branches: [ "main" ]
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ./hello-world-docker-action
          with:
            who-to-greet: 'Felipe'
